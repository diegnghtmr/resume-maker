% Tailored application CV. INLINE the tailored bullets below so this archive never drifts;
% only the stable header/links are \input.
%
% Build from the repo ROOT:
%     scripts/build.ps1 applications/<folder>/main-en.tex      (or scripts/build.sh)
%   -> build/applications/<folder>/main-en.pdf
%
% NEVER run bare `latexmk` on this file: it obeys ./latexmkrc, writes to build/cv/, and
% OVERWRITES the canonical main CV. Only the scripts pass a per-source-dir -outdir.
%
% Rule: reshape facts from content/<lang>/*.tex; never invent new ones.
\documentclass[letterpaper,12pt]{fed-res}
\input{shared/links.tex}
\begin{document}
\input{content/en/header.tex}          % or content/es/header.tex

\section{Professional Profile}
% Rewrite/trim the profile for this role (facts from content/en/profile.tex).

\section{Work Experience}
\resumeSubHeadingListStart
    % Reorder/reword entries from content/en/work.*.tex to match the posting.
    \resumeSubheading
        {Role}{Date range}
        {Employer}{City, Region}
        \resumeItemListStart
            \resumeItem{Tailored, quantified bullet aligned to the job.}
        \resumeItemListEnd
\resumeSubHeadingListEnd

\section{Skills}
% Emphasize the skills the posting asks for (from content/en/skills.tex).

\end{document}
